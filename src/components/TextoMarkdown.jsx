// Esqueleto do texto do card — a assinatura existe para que o teste do
// ciclo 3 falhe na asserção, e não no `import`.
import React from 'react';

export default function TextoMarkdown(_props) {
  return null;
}
