// A lista de conversas diretas.
//
// PASSO 1: a v0.7.0 não tem conversa direta nenhuma. O incumbente é o vazio —
// uma lista que não mostra nada. É contra ele que o teste está vermelho.
import React from 'react';

export default function ListaConversas() {
  return <ul className="conversas" />;
}
