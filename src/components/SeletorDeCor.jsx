// Esqueleto do seletor de cor — as assinaturas existem para que o teste do
// ciclo 4 falhe na asserção, e não no `import`.
import React from 'react';

export const ROTULO_AUTOMATICA = 'Automática';

export default function SeletorDeCor(_props) {
  return null;
}
