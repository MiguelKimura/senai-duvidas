// Esqueleto do painel avancado — as assinaturas existem para que o teste do
// ciclo 5 falhe na asserção, e não no `import`.
import React from 'react';

export const ROTULO_DO_PAINEL = 'Opções avançadas';

export default function PainelAvancado(_props) {
  return null;
}
