// Esqueleto da preferência de cor — as assinaturas existem para que o teste
// do ciclo 6 falhe na asserção, e não no `import`.
export const CHAVE_DA_COR = 'senai-duvidas:cor-do-card';

export function lerCorPreferida() {
  return null;
}

export function guardarCorPreferida(_cor) {}
