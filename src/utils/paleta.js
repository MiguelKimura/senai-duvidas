// Esqueleto da paleta — as assinaturas existem para que o teste do ciclo 1
// falhe na asserção, e não no `import`.
export const CONTRASTE_MINIMO = 4.5;
export const PALETA = [];
export function razaoContraste(_a, _b) {
  return 0;
}
export function corDaPaleta(_cor) {
  return null;
}
